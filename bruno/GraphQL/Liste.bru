meta {
  name: Liste
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/graphql
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "query": "query Fahrzeuge { gebrauchtwagenListe(input: { page: 1, size: 5 }) { total page size data { id marke modell } } }"
  }
}
