# Task Description

Create a simple public repository for a Go prototype. Add `README.md`, a `proto-go` directory with `main.go`, `db.go`, `go.mod`, `Makefile`, and run scripts (`run.ps1`, `run.sh`). Include a `TASK_DESCRIPTION.md` and submission ReadMe. No CI required. Keep files minimal and runnable with `go run` or Docker.
